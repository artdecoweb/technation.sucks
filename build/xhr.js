/**
 * @typedef {{ user: User, csrf: string, positions: Array<Position> }}
 */
var Auth

/**
 * @typedef {{ id: string, firstName: string, lastName: string, profilePicture: string } }}
 */
var User

/**
 * @typedef {{ id: number, name: string, title: string, location: string } }}
 */
var Position